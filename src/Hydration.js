// What is this class doing??
// You can probably just store the return value in a const when you need it

class Hydration {
  constructor(hydrationData) {
    this.hydrationData = hydrationData;
  }  
}

export default Hydration;
