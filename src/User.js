if (typeof module !== "undefined") {
    userData = require("../data/user-test-data");
  }

class User {
    constructor() {
        
    }
}











if (typeof module !== 'undefined') {
    module.exports = User;
  }