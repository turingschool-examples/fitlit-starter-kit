if (typeof module !== "undefined") {
    userData = require("../data/users-test-data");
  }

class UserRepo {
    constructor() {
        
    }
}


if (typeof module !== 'undefined') {
    module.exports = UserRepo;
  }