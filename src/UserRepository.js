class UserRepository {
    constructor(users = []) {
        this.users = users;
    }
    avgStepGoal() {
        
        //need to find the average step goal between users
        //need array.length
    }
}

export default UserRepository;