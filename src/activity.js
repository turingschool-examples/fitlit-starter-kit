class Activity {
  constructor(data) {
    this.data = data;
  }

  // calculateAvgMinActive(date, id) {
  // let userActivity = this.data.filter(user => user.userID === id);
  // console.log(userActivity.reduce((acc, currentValue) => {
  //   return
  // }))
  // }

  calculateMilesWalked(date) {
    console.log(this.data);
  }
}

module.exports = Activity;