class Dashboard {
  constructor(currentUser) {
    this.currentUser = currentUser;
  }

  displayCurrentInfo() {
    // Create an info card on the dashboard with all of user’s info on the page
  }

  displayFirstName() {
    // call user.getFirstName and send name to DOM
  }

  compareStepGoal() {
    // user step goal compared to total useres step goal
  }

}