
//Find numSteps of all time for a user
	//Use forEach to compare one index of steps to the two before it
	//Conditional - if it's increasing >= 3, push highest index into an array
	//Return array of dates and highest steps

	//Sleep Metric
		//Average hours slept for a week


	//Activity Metric
		//calories burned

	//Trend Metric
		//user meeting goal for all three categories